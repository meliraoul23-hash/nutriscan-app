// Colors - Export from premium theme
export { 
  premiumTheme as theme,
  colors,
  getScoreColor,
  getScoreColor as getScoreGradient,
  getNutriColor as getNutriScoreColor,
  getAdditiveRiskColor as getAdditiveColor,
} from './premiumTheme';
